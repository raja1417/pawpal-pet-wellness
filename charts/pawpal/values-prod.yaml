api:
  replicaCount: 3
  autoscaling:
    minReplicas: 3
    maxReplicas: 10
web:
  replicaCount: 3
  autoscaling:
    minReplicas: 3
    maxReplicas: 10
ingress:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
config:
  nodeEnv: production
postgresql:
  enabled: false
